"""
Unit and regression test for the durf_hq package.
"""

# Import package, test suite, and other packages as needed
import durf_hq
import pytest
import sys

def test_durf_hq_imported():
    """Sample test, will always pass so long as import statement worked"""
    assert "durf_hq" in sys.modules
